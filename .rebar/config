{plugin_dir, "~/.rebar/plugins"}.
{plugins, [dierl]}.

{dialyzer_opts, [src]}.

{erl_opts, [bin_opt_info]}.

{edoc_opts,
	[ no_packages
	, {hidden, true}
	, {private, true}
	, {pretty_printer, erl_pp}
	]}.

{eunit_opts, [{report, {eunit_surefire, []}}]}.
{cover_enabled, true}.
